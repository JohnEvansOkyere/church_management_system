# Copy to Vercel → Project → Settings → Environment Variables (Production + Preview).
# Must include /api/v1 — the axios client expects the API prefix here.

VITE_API_BASE_URL=https://your-service-name.onrender.com/api/v1
